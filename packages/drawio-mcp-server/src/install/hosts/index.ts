import { HostAdapter } from "../types.js";

// Empty adapter registry for now. Task 2 will register the `codex` adapter,
// with further host adapters landing in subsequent tasks.
export const hostAdapters: HostAdapter[] = [];
